[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "final-project"
version = "0.1.0"
description = "Final Project skeleton for VO codebase"
authors = ["<your-name>"]

[tool.pytest]
addopts = "-q"
